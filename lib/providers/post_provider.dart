import 'package:flutter_riverpod/flutter_riverpod.dart';

final postButtonProvider = Provider((ref) {
  return {
    // Firestoreにデータを保存するためのロジック
  };
});

final repostButtonProvider = Provider((ref) {
  return {
    // Firestoreにデータを保存するためのロジック（RePostの場合）
  };
});
